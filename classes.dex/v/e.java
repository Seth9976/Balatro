package v;

import androidx.fragment.app.Fragment;

public final class e extends f {
    public e(Fragment fragment0) {
        k3.f.e(fragment0, "fragment");
        super(fragment0, "Attempting to get target fragment from fragment " + fragment0);
    }
}

